## MODIFIED Requirements

### Requirement: Edit button opens edit modal
The edit button in the position detail view header SHALL open the `EditApplicationModal` with the current position data when clicked.

#### Scenario: User clicks edit in detail view
- **WHEN** the user clicks the "Edit" button in the detail view header
- **THEN** the `EditApplicationModal` opens pre-filled with the position's data
